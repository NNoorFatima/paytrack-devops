{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:3000",  // Your React app URL
      "webRoot": "${workspaceFolder}/src", // Adjust if needed
      "sourceMaps": true,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
