apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paytrack.fullname" . }}-test-connection"
  labels:
    {{- include "paytrack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paytrack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
